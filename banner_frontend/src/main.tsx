import React from 'react'
import ReactDOM from 'react-dom/client'
import BannerPage from './BannerPage.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BannerPage />
  </React.StrictMode>,
)
